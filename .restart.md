# Mahalath Restart Notes

Last updated: 2026-06-06

## Status

Project bootstrap. No code yet.

Domain created at:

```text
/home/cello/domains/Mahalath
```

Repo cloned from `https://github.com/gellsmore-svg/mahalath` at commit
`4456a45 Initial commit` (a fresh repo containing only the LICENSE and
a placeholder README).

Operator-supplied initial requirements have been captured verbatim in
`docs/requirements-v0.1.md`.

Conventions in use, borrowed from Mnemosyne:

- `.restart.md` (this file) is canonical *current state*; `.session-log.md`
  is *how we got there*, append-only chronological.
- ADR-style architecture decisions go in `docs/architecture-decisions.md`
  once that file exists; open questions go in `docs/open-questions.md`.
- Codex pushes from the WSL root user. Claude works as cello and commits
  locally; codex handles `git push`.
- Standing rules of the road are not yet written; see CONTRIBUTING.md
  for the working convention.

## Next Step

Continue the bootstrap plan:

- Chunk 3 — write `docs/project-brief.md` and `docs/architecture-decisions.md`
  with the obvious accepted decisions (Python, MongoDB, Ollama adapter,
  watched folder, hybrid flat+tree ontology, hierarchical numeric
  labels, 25-iteration cap, confidence threshold 8.0) and the unresolved
  questions.
- Chunk 4 — set up the Python project skeleton: `pyproject.toml`,
  `.gitignore`, `src/mahalath/` package, `tests/`, `config.example.yaml`,
  the five runtime folders (`input/`, `processed/`, `ontology/`, `logs/`,
  `undecided/`) with `.gitkeep` stubs.
- First actual code is held until operator review of chunks 1–4.

Standing rules from the operator (carried over from the Mnemosyne
pattern, applicable here by default until contradicted):

- Keep semantic / ontology writes review-based at first; do not grant
  agent autonomous write authority before review queues and audit logs
  exist.
- Keep source-preserved: do not silently rewrite, compress, or summarise
  source documents during ingestion.
- Ask before implementing changes that alter source preservation, agent
  write authority, or any of the named "Out of scope" items in
  `docs/requirements-v0.1.md` §7.

## Current Files

- `README.md` — placeholder (10 bytes at clone).
- `LICENSE` — standard.
- `.restart.md` — this file.
- `.session-log.md` — chronological session narrative; first entry
  2026-06-06 [claude].
- `docs/requirements-v0.1.md` — operator-supplied initial requirements,
  captured verbatim.
- `CONTRIBUTING.md` — working convention for codex/Claude coordination
  and the project's restart-doc discipline.
