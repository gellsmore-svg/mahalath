# Mahalath Restart Notes

Last updated: 2026-06-13 (S2.51 cross-language FINGERPRINTING for
candidate shortlisting — adapter `embed()`, `embeddings.py`
[backfill + cosine + shortlist], `entry_embeddings` collection,
`generate-mappings --candidate-source {auto,embedding,prompt}`,
`backfill-embeddings` CLI; PLUS v1 fresh-install bootstrap
`mahalath init` and `docs/v1-scope.md`. LIVE-PROVEN on real bge-m3
(2026-06-13): embed reaches the Windows daemon at the WSL2 gateway IP
(NOT localhost; needs OLLAMA_HOST=0.0.0.0 — config.yaml set, gateway
IP can drift across WSL reboots); 117/119 entries embedded; the
shortlist now ranks coupling#1 for Kopplung + phase#1 for
Phasenverschiebung (the pairs the prompt stage missed 3x). Added
NaN-resilience (bge-m3 emits NaN on some inputs → def-only fallback,
then graceful skip; 2 en entries un-embeddable). Prior: S2.50 re-debate path — `redebate-entry`
CLI + `ontology.redebate_entry`; the 8 single-model German pilot
entries MPL-111..118 refreshed cross-family [mistral vs gemma],
gemma originals kept in history, Kopplung's two M-B-flagged grammar
errors fixed; staleness cascade bug found+fixed mid-slice so 24
mappings on the re-debated endpoints are now correctly stale for
re-audit. NEXT UP: build the meaning-fingerprint candidate
shortlisting for M-C, operator-approved Mahalath-local + multilingual
model. Prior: S2.49 mapping resolution lifecycle —
`resolve-mapping` CLI + decided_via provenance; both Kopplung
unresolved pairs adjudicated REJECTED as delegate, mappings 25
rejected / 2 accepted / 0 unresolved. Prior: S2.48 M-C
gate relaxed to MAJORITY verdict + median-across-typed-votes
confidence, operator-ruled, ADR-029 amended; **M-C functionally
complete and proven** — first APPLIED de→en run, Mahalath's first
cross-language mappings live [MPL-115→054, MPL-119→070, both
partial_overlap conf 8.5], see the 2026-06-13 session-log entries +
`logs/mappings-apply-de-en-20260612T2332Z.json`). Earlier same evening:
S2.45 de pilot passed; S2.46 multi-model debate + cross-family
consensus roster, DQ-010 closed; S2.47 M-C machinery shipped + first
DRY-RUN (0/14/13, gate held). Earlier: S2.44 — multilingual M-A shipped: entry-level language, per-lexicon guard/search/tree boundaries, text-index override decoupled, live data stamped 'en'. Earlier: S2.41 known-term guard; S2.42 scale correction; S2.43 decided_via; ADR-028/029/030 multilingual rulings. Also: S2.40 — the redefine path now ends with a
scoped intent backfill over the entries it rewrote, pipeline-tail
pattern, `--no-intent-backfill` opts out; self-healing no longer
reopens the intent-coverage gap. Earlier same day: S2.39 shipped the
decision-effectiveness self-analysis closing requirements §3.4
(`analysis.py` + `mahalath effectiveness` + web `/effectiveness` +
`GET /api/effectiveness` + nightly snapshot to
`logs/effectiveness.jsonl` from the REM tail; ADR-027), whose first
live report found 42/100 rs_bachelor entries stale — backlog since
CLEARED to 0 and fully intent-annotated, see "Operational state".
Also earlier same day, S2.38:
intent extension I-D gate PASSED; intent attribution applied across
all 9 DBs; the intent extension I-A..I-D is COMPLETE. Ollama note:
the 0.30.7 auto-update
was incompatible with NVIDIA driver 546.30 ("CUDA error: device kernel
image is invalid"); operator rolled back to 0.23.4 from the original
installer on 2026-06-12 and everything works — gemma4:e2b enumerates
and generates again. Beware future Ollama auto-updates until the
driver is updated.)

This is the canonical "current state" doc. The blow-by-blow of *how we
got here* lives in `.session-log.md`; this file says *where we are*.

## Status

**Stage 1 is complete and proven; Stage 2 is deep (through S2.39).**
The system ingests a corpus, debates precise definitions, grows a
hierarchy, tracks cross-references, self-heals stale entries, answers
grounded questions over the ontology, exports a glossary, serves a
retrieval API, annotates intent, and audits its own decision-making
nightly — all local-first against MongoDB + the Windows Ollama binary.

Tests: **415 passing** (was 73 at Stage 1 end). Includes live MongoDB
round-trips against a `mahalath_pytest` database (dropped before/after).

### Commands working (`mahalath <cmd>`)

Core pipeline:
- `db-ping`, `show-config`
- `ingest-one <path>` — SHA-256 dedupe, archive to `processed/`, record.
- `process-document <id> [--max-terms N] [--consensus-passes N]
  [--no-hierarchy-review]` — extract → debate → persist → hierarchy review.
- `process-input` — idempotent watched-folder scan + pipeline.
- `list-ontology`, `export-glossary [--format md|json] [--out PATH]`.
- `debate-one` — ad-hoc single-term debate.

Hierarchy / proposals:
- `list-proposals [--status S]`, `show-proposal`, `accept-proposal`,
  `reject-proposal`, `rollback-proposal` (revalidate-on-apply; rollback
  via stored `previous_parent_label`).

Definition contexts (polysemy) + intent taxonomy (I-A/I-B):
- `list-contexts [--kind frame|intent]`, `add-context <name> --description D
  [--kind frame|intent]`, `show-context <id|name>`,
  `seed-intents [--dry-run]` (idempotent standard illocution tags),
  `backfill-intents [--max-items N] [--apply] [--passes N] [--adapter A]
  [--model M]` — N-pass unanimity attribution, dry-run by default.
- `backfill-contexts [--max-items N] [--apply] [--adapter A] [--model M]`
  — walk untagged definitions, ask the model to frame each with one of
  the defined contexts; dry-run by default, `--apply` writes + refreshes
  the glossary. (S2.26.)

Staleness / self-healing:
- `list-stale`, `backfill-references`, `audit-stale`, `redefine-stale`
  (S2.40: redefine now ends with a scoped intent backfill over the
  entries it rewrote — pipeline-tail pattern; `--no-intent-backfill`
  opts out).

Self-analysis (§3.4, S2.39):
- `effectiveness [--format text|json] [--snapshot]` — decision-
  effectiveness report: debate outcomes, operator-verdict-vs-agent-
  confidence calibration bands, undecided-queue health, re-debate
  resolution arcs, hierarchy-review yield, coverage, plus rule-based
  plain-language findings. `--snapshot` appends a JSON line to
  `logs/effectiveness.jsonl` (the REM nightly tail does this
  automatically).

Frontier review + serving:
- `frontier-review` — LLM adjudicator over the pending_review queue.
- `serve` (FastAPI web UI + `/api/chat`), `run`.

### Modules under `src/mahalath/`

- `config.py` — pydantic config tree + YAML loader. Note: `/api/chat`
  default `chat_adapter` is **`ollama_cli`** as of S2.26 (was
  `claude_api`); set back to `claude_api` (+ `ANTHROPIC_API_KEY`) for
  frontier-quality chat.
- `labels.py` — MPL label parse/format (`MPL-NNN[.NNN...][a-z]?`).
- `db/` — client, indexes, Pydantic models, repositories. Collections:
  `documents`, `ontology_entries` (`_id` = MPL label), `ontology_tree`,
  `decision_log`, `agent_exchanges`, `undecided_queue`,
  `action_proposals`, `ontology_reviews`, `definition_contexts`.
- `adapters/` — `Adapter` protocol, `MockAdapter`, `OllamaCliAdapter`,
  `ClaudeApiAdapter`, `factory.make_adapter`.
- `extraction.py`, `chunking.py` — candidate-term extraction with
  heading-aware Markdown chunking; "term must appear in document" filter.
- `debate.py` — bounded PrecisionCritic / SynthesisExplorer loop,
  `min(pc, se)` confidence, 50-iteration cap.
- `ontology.py`, `hierarchy.py`, `actions.py`, `proposals.py` —
  persistence, hierarchy review/consensus, agent-callable structural
  actions, operator proposal queue.
- `staleness.py` — reference tracking, cascade staleness, audit/redefine
  self-healing, **and `backfill_definition_contexts`** (the S2.26 core).
- `style.py` — style-prompt overlay (per ADR; biggest quality lever).
- `glossary.py` — export + `refresh_glossary` cache.
- `chat.py` — grounded NL Q&A over the ontology, context-grouped prompts,
  tool-call action proposals.
- `retrieval.py` — typed read view (S-A..S-D): `score_entry` (shared
  ranker), `search_terms`, `get_codified`, `subtree`, `build_bundle`
  (token-budgeted + reference-closed), `render_entry_lines` (the ONE
  text renderer — chat context delegates to it), `propose_term` (the
  module's one write path → undecided queue). CLI: `retrieve` (bundle
  by default; `--budget`, `--format json|text`, `--matches-only`),
  `subtree`, `propose-term`. HTTP: `POST /api/retrieve`,
  `POST /api/propose_term`.
- `paths.py` — materialised `OntologyEntry.path` maintenance:
  `compute_path`/`set_path`/`propagate_paths`/`resolved_path` +
  `backfill_paths` migration (CLI `backfill-paths`).
- `analysis.py` — decision-effectiveness self-analysis (§3.4, S2.39;
  ADR-027 read-only): `build_effectiveness_report` (server-side
  aggregations → DebateStats / ProposalStats+CalibrationBands /
  QueueStats / RedebateStats / ReviewStats / CoverageStats + findings),
  `render_report_lines`, `report_to_dict`,
  `write_effectiveness_snapshot` (appends logs/effectiveness.jsonl;
  called from the REM nightly tail).
- `frontier.py`, `rem.py`, `scheduler.py` — frontier adjudication, REM
  consolidation, APScheduler wrap (+ nightly effectiveness snapshot).
- `web/app.py` — FastAPI UI + JSON API (incl. `/effectiveness` +
  `GET /api/effectiveness`).
- `cli.py` — argparse entry point wiring all of the above.

## Operational state (live databases)

**ONE live database as of 2026-06-12: `mahalath_rs_bachelor`** — the
RS lexicon, 110 entries, fully frame-tagged, intent-annotated, path-
backfilled, 0 stale. The operator had the 8 others dropped after the
lexicon merge (below): `gcse` + `bachelor` were superseded by the
merge; `dev`, `style`, `rem`, `pinput`, `autoexport`, `multi` were
feature-test scratchpads. (`mahalath_pytest` remains transient —
created/dropped by the test suite.) A local gitignored `config.yaml`
now points the CLI at `mahalath_rs_bachelor` and sets the RS style
overlay, because config.example.yaml's default `mahalath_dev` no
longer exists; repoint both when a new corpus moves in. Historical
"all 9 DBs" notes in older entries below/in the session log refer to
the pre-drop fleet.

**RS lexicon MERGED 2026-06-12 (operator direction; RS content is
test data — the point was proving one-lexicon/many-documents):**
mahalath_rs_bachelor is now the single RS lexicon at **110 entries**.
`bachelor` was the same source doc (early 10-term pass; 8/10 terms
covered); `gcse` was re-ingested as a second document through the
pipeline — the S2.41 known-term guard turned 33 overlapping candidates
into provenance on existing entries and 10 GCSE-level concepts were
debated in (MPL-101..110). `mahalath_gcse` + `mahalath_bachelor` are
fully superseded; all scratch DBs droppable (operator action).
Operator review items: MPL-101 "Relational Primacy" vs MPL-013
"relational-primacy claim" (alias/merge or distinct?); 2 pending
hierarchy proposals; 2 withheld intent attributions; 'Coherent
Biblical Ontology' not enqueueable via propose_term (fail-closed on
MPL-048 coverage) — operator-author or alias if wanted.

**rs_bachelor stale backlog CLEARED 2026-06-12** (found by the S2.39
effectiveness report: 42/100 entries stale after the intent-rollout/
redefine cascades). Manual library run with the corpus style overlay:
42 audited (0 errors) → 11 cleared as still-consistent, 29
audit-inconsistent entries redefined against current upstream (conf
9.5–10.0), glossary refreshed. The final 2 (MPL-063, MPL-096 — gemma4:e2b
couldn't clear the 6.0 redefine floor on them) were then redefined at
frontier quality per operator instruction: ANTHROPIC_API_KEY isn't
configured, so Claude authored the responses in-session against the
exact build_redefine_prompt output and fed them through the standard
redefine path via a scripted adapter (parse → floor → frame resolution
→ persist → update_references → clear_stale). **0 stale remaining.**
Note their `model_used` reads "rem_redefine" (hardcoded by the path);
true provenance is in the 2026-06-12 session-log entry.

The CLI has no `--database` flag — the local `config.yaml` selects the
DB (currently `mahalath_rs_bachelor`); for ad-hoc cross-DB work, call
the library functions directly with `cfg.mongo.database` set.

### Multi-model debate (S2.46, DQ-010 closed)

Live config: PrecisionCritic=mistral-small vs SynthesisExplorer=
gemma4:e2b (cross-family min(pc,se)); consensus_models=[gemma4:e2b,
mistral-small, qwen3.5:35b] rotates hierarchy + intent passes across
three families. Cost: mistral ~52-62s/call, qwen ~72s/call vs gemma
~4-14s — drop the roster to [gemma4:e2b] in config.yaml for bulk runs.

## Next-slice candidates

(Stages 1 → 2.26 are all landed; full history in `.session-log.md`.)

- ~~Run `backfill-contexts` automatically as part of `process-document`~~
  **done (S2.27)**: the pipeline runs a context backfill scoped to the
  entries it accepted this run, catching definitions the debate left
  untagged. `--no-context-backfill` opts out.
- ~~Web UI surface for definition contexts~~ **done (S2.28)**:
  `/ontology/{label}` groups definitions by frame (one section per
  context with its description), badges untagged definitions, and shows a
  polysemy note when a term spans multiple co-equal frames. The
  `/ontology` list (S2.29) carries a Contexts column that badges entries
  with untagged definitions so they are findable without opening each.
- ~~Decision-effectiveness dashboard (§3.4)~~ **done (S2.39)**:
  `analysis.py` + `mahalath effectiveness` + `/effectiveness` +
  `GET /api/effectiveness` + nightly snapshot (ADR-027). Remaining
  §3.4-adjacent refinement: trend rendering OVER the accumulated
  jsonl snapshots (the data accrues nightly; nothing reads it back
  yet).
- `debate-one` CLI command is still a "not yet implemented" stub.
- **Retrieval layer** (`docs/retrieval-spec.md`, ADR-020–023): a typed
  read-view API (`retrieval.py`) letting an LLM query the ontology for
  codified refs + prompt-ready bundles. **S-A done (S2.30)**:
  `score_entry`/`search_terms`/`get_codified` + `$text` index +
  `mahalath retrieve`. **S-B done (S2.31)**: materialised
  `OntologyEntry.path` (maintained on insert/reparent/rollback via
  `paths.py`), `backfill-paths` migration, `retrieval.subtree()` +
  `mahalath subtree`. **S-C done (S2.33)**: `build_bundle`
  (token-budgeted, reference-closed, recorded degradation ladder,
  frame-scoped handles honoured), shared `render_entry_lines` text
  renderer (chat context now delegates to it), `mahalath retrieve`
  bundle output (`--budget`/`--format`), `POST /api/retrieve`.
  **S-D done (S2.34)**: `propose_term` (retrieval's one write path —
  no-match terms enqueue onto the EXISTING undecided path and flow
  through unmodified REM re-review; `mahalath propose-term`,
  `POST /api/propose_term`) + `DefinitionVersion.consensus_score`
  (per-definition `min(pc, se)` snapshot at accept; None for
  operator/redefine/legacy). Q2 + Q3 resolved. Remaining: S-E
  (optional) Tirzah semantic backend only. Subsumes the old NL↔MPL
  translation (DQ-008) idea.
- **Intent extension — COMPLETE (I-A..I-D, S2.35–S2.38; ADR-024/025/
  026).** Schema + taxonomy, the N-pass unanimity ingestion gate
  (`attribute_intent`, `backfill-intents`, pipeline tail with
  `--no-intent-backfill`), the read surfaces (`Filters.intent_tag`
  fails closed; intent fields on `Meaning` + glossary; "(deployed
  to: …)" asides in bundle + chat text; web badges), and the **I-D
  gate PASSED on 2026-06-12**: 15/15 dry-run attributions unanimous on
  gemma4:e2b across rs_bachelor + style corpora, intersection gate
  visibly dropping minority tags and disagreed ordinals. Attribution
  then APPLIED across all 9 DBs (below-threshold items withheld for
  operator review, as designed). Watch-item: tag distribution skews
  `teach` on teaching corpora (genre projection signal);
  intentionality discriminates across corpus types, not within one
  book. New entries get attributed automatically at the pipeline tail.
- **Code-review backlog (2026-06-11, recorded not fixed — all
  scale-dependent):** N+1 `all_labels()+get()` pattern in
  search_terms / chat selection / glossary gather / backfills (fix:
  single-cursor `repo.all()`); `extract_semantic_references` makes
  `backfill_references` O(vocab²) (alternation regex / Aho-Corasick when
  it hurts); `mark_dependents_stale` recursion → stack; retrieval-spec
  `Filters` lists `updated_after`/`document_label` that aren't
  implemented (decide in S-C); split `cli.py` (~1,800 lines) around
  S-D. Bugs already fixed in S2.32: scorer label over-match, glossary
  frame export, reverse-ref retro-linking on insert, subtree-via-path +
  `path` index.
- **Multilingual lexicons (ADR-028/029/030, accepted 2026-06-12;
  design discussed inline, operator-ruled).** Phases, each gated on an
  operator go: ~~M-A~~ **done (S2.44)**: entry-level `language` on
  entries + documents (inherited from source doc), cross-language
  parent/merge rejected, known-term guard per-lexicon,
  Filters.language + --language flags + API passthrough, compound
  (language, canonical_term) index, text index rebuilt with
  language_override="text_language" (semantic codes can never fail
  inserts; zh proven), backfill-language run live. **M-B** first non-English pilot
  corpus end-to-end in the same collection (gate: debate quality
  spot-check; mitigation for small-model language quality is
  multi-model debate — qwen/gemma/mistral — which finally motivates
  the old §3.6/DQ-010 concurrent-multi-model gap). ~~M-B~~ **done
  (S2.45, gate passed with notes)**: de pilot lexicon live (MPL-111..
  119, 9 entries after S2.46's Rückkopplung). ~~M-C machinery~~
  **shipped (S2.47)**: mappings.py per ADR-029 — governed relation
  taxonomy (4 seeded), candidate scouting, N-pass cross-family
  unanimity gate, illocution comparison, staleness participation,
  `seed-mapping-relations`/`generate-mappings`/`list-mappings`.
  ~~M-C gate~~ **relaxed + proven (S2.48, operator-ruled
  2026-06-13)**: dry-run showed strict unanimity+min() stalling the
  two true pairs on partial_overlap-vs-narrower_than splits, so the
  gate now takes the MAJORITY verdict (none included) with
  confidence = median across typed votes (ADR-029 amended). First
  APPLIED de→en run: **2 accepted / 23 rejected / 2 unresolved**.
  Mahalath's first cross-language mappings are LIVE — MPL-115
  Resonanz→MPL-054 Resonance and MPL-119 Rückkopplung→MPL-070
  Feedback, both partial_overlap conf 8.5. mappings collection now
  has 27 rows. ~~Adjudicate the 2 Kopplung unresolved pairs~~ **done
  (S2.49)**: built the mapping resolution lifecycle step
  (`resolve_mapping` + `resolve-mapping` CLI + Mapping.decision_*
  fields, mirroring S2.43 decided_via; preserves the model
  per_pass/models_used audit). Both Kopplung pairs (MPL-117→001,
  MPL-117→003) adjudicated REJECTED via decided_via=operator_delegate
  — scout false-friends on "relation"/"resonance-constraint" lexical
  pulls, not real meaning relations; mistral-small had them right.
  **mappings now: 25 rejected / 2 accepted / 0 unresolved.** Other
  follow-ups (none blocking): illocution divergence on the accepted
  pairs is currently a genre-projection artifact (de pilot is
  teaching-overlay; revisit with a non-teaching de corpus); CLI knobs
  for max_target_snapshot (60) + candidate cap (3/source) not yet
  exposed. **Scouting recall is the
  one real weakness — three runs missed both planted pairs (MPL-039
  phase, MPL-038 coupling); S-E embeddings as candidate stage is the
  firm next M-C dependency before lexicon-scale runs.** Details in
  session log + `logs/mappings-{dryrun,apply}-de-en-*.json`.
- Full overnight RS run with overlay at `--max-terms 100`.
- Tirzah cross-pollination (memory-retrieval sibling, formerly
  Mnemosyne): Mahalath's accepted entries could seed its labeled-corpus
  retrieval. Code-reading per ADR-014; no runtime coupling. Standing
  rule: codex owns Tirzah; Claude does not touch its repo without an
  explicit go-ahead.
- **Tirzah context-collation integration — V2 FEATURE, design agreed
  2026-06-13 (operator), not yet built.** Purpose: improve the QUALITY
  of the context fed into a debate by collating a candidate word's
  whole-document context before defining it — "calibrate the input so
  the lossy (quantized-model) compression produces a better definition"
  (operator's analogy). This is a DIFFERENT capability from the local
  meaning-fingerprint work (which is for cross-language candidate
  shortlisting): fingerprints find similar pieces; Tirzah collates them
  into a higher-fidelity context. Agreed shape:
  - **Synchronous** request from Mahalath to Tirzah during ingestion
    (operator chose live request over an offline hand-off).
  - A **Mahalath config setting** selects the context mode, trading
    performance vs quality: **(a) none** — no Tirzah, current
    local-section behaviour (fastest); **(b) fast / simple python
    request** — mechanical similarity retrieval, no LLM in Tirzah's
    loop; **(c) llm-brokered chunk compilation** — Tirzah uses a model
    to synthesise the chunks (best quality, slowest).
  - Fixes ACCURACY (is the definition true to the document?), NOT
    fluency (the German grammar slips need the debate self-check, a
    separate lever).
  - When built: needs a new ADR scoping ADR-014 (this is an OPT-IN
    runtime dependency on Tirzah, reversing strict independence for
    modes b/c); must define ingestion's fallback when Tirzah is
    unavailable/times out; and note that mode (c) stacks another
    local-LLM call into ingestion — slow on current hardware (see the
    ≥600s Ollama timeout reality). What Tirzah exposes (a queryable
    store? a "document+term → collated context" call?) is the open
    detail that fixes the request contract.

## Standing Rules

Carried from the operator pattern, applicable until contradicted:

- Keep semantic / ontology writes review-based; do not grant agents
  autonomous write authority before review queues and audit logs exist.
  (Done: every accepted entry has a `decision_log_id`; undecided +
  proposal queues exist.)
- Keep source preserved: do not silently rewrite, compress, or summarise
  source documents during ingestion. (Done: source bytes copied verbatim
  to `processed/`, SHA-256 verified post-copy.)
- Ask before implementing changes that alter source preservation, agent
  write authority, or any named "Out of scope" item in
  `docs/requirements-v0.1.md` §7.

## Current Files

Root:
- `README.md`, `LICENSE`, `CONTRIBUTING.md`
- `.restart.md` (this file), `.session-log.md`
- `pyproject.toml`, `.gitignore`, `config.example.yaml`

`docs/`:
- `requirements-v0.1.md`, `project-brief.md`
- `architecture-decisions.md` (through ADR-023)
- `retrieval-spec.md` (accepted design; not yet implemented)
- `style-overlay.example.md`
- `terminology.md` — plain-language glossary of Mahalath's OWN working
  vocabulary (debate, mapping, shortlist/"scouting", fingerprint, etc.);
  distinct from the corpus glossary `export-glossary` produces. Keep it
  current when new project terms appear.

`src/mahalath/`:
- `__init__.py`, `py.typed`, `config.py`, `labels.py`, `cli.py`
- `ingestion.py`, `extraction.py`, `chunking.py`, `debate.py`,
  `ontology.py`, `hierarchy.py`, `actions.py`, `proposals.py`,
  `staleness.py`, `style.py`, `glossary.py`, `chat.py`, `retrieval.py`,
  `paths.py`, `intents.py`, `analysis.py`, `frontier.py`, `rem.py`,
  `scheduler.py`
- `db/{__init__,client,indexes,models,repositories}.py`
- `adapters/{__init__,base,mock,ollama_cli,claude_api,factory}.py`
- `web/{__init__,app}.py`

`tests/` (30 modules): `conftest.py` (`mongo_db` fixture) plus
`test_{actions,adapters,analysis,bundle,chat,chunking,config,db_models,
debate,definition_contexts,extraction,frontier,glossary,
glossary_refresh,hierarchy,ingestion,intent_valence,intents,labels,
ontology,paths,pipeline_backfill,proposals,rem,retrieval,scheduler,
staleness,style,style_resolve,web}.py`.

Runtime folders: `input/`, `processed/`, `ontology/`, `logs/`,
`undecided/` (`.gitkeep` stubs; contents gitignored).
